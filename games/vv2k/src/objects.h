#include <sstream>

class Concepts {
    public:
        class line {
            public:
                bool transition;
                bool left;
                int img = 0;
                char text[198];
        };
};